[customizations.installer.kickstart]
contents = """
# Kickstart for unattended installation with GNOME defaults

# Accept the EULA automatically
eula --agreed

# Partition strategy: use LVM and automatic partitioning (entire disk)
autopart --type=lvm --fstype=ext4

# Accept the default storage configuration without prompts
ignoredisk --only-use=sda,sdb,nvme*

# Network - use DHCP
network --bootproto=dhcp --onboot=on --activate

# Root password (set to a strong password or use --lock to disable root login)
rootpw --lock

# Don't prompt for partitioning
zerombr

# Clear all partitions
clearpart --all --initlabel

# Skip initial setup on first boot
firstboot --disable

# Reboot after installation
reboot

%post
bootc switch --mutate-in-place --transport registry ghcr.io/ublue-os/image-template:latest
%end
"""

[customizations.installer.modules]
enable = [
  "org.fedoraproject.Anaconda.Modules.Storage",
  "org.fedoraproject.Anaconda.Modules.Runtime",
  "org.fedoraproject.Anaconda.Modules.Payloads"
]
disable = [
  "org.fedoraproject.Anaconda.Modules.Network",
  "org.fedoraproject.Anaconda.Modules.Security",
  "org.fedoraproject.Anaconda.Modules.Services",
  "org.fedoraproject.Anaconda.Modules.Users",
  "org.fedoraproject.Anaconda.Modules.Subscription",
  "org.fedoraproject.Anaconda.Modules.Timezone",
  "org.fedoraproject.Anaconda.Modules.Localization"
]
