{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "./groups.schema.json",
  "title": "Groups",
  "description": "Domain-scoped source of truth for group configurations. Edited via the web UI and read directly by the build pipeline.",
  "$comment": "Cross-field membership check for printer.default_printer against dynamic no_ppd/with_ppd keys is enforced by scripts/validate-group-vars.py.",
  "type": "object",
  "additionalProperties": false,
  "required": ["domains"],
  "properties": {
    "domains": {
      "type": "array",
      "description": "List of domains. Groups are isolated per domain.",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["domain", "groups"],
        "additionalProperties": false,
        "properties": {
          "domain": {
            "type": "string",
            "description": "Owning domain for the enclosed groups.",
            "pattern": "^[a-z][a-z0-9_-]*$",
            "minLength": 1
          },
          "groups": {
            "type": "array",
            "description": "List of configuration groups for this domain.",
            "minItems": 1,
            "items": {
              "type": "object",
              "required": ["name"],
              "additionalProperties": false,
              "properties": {
          "name": {
            "type": "string",
            "description": "Unique group identifier. Used as the image name in the build pipeline.",
            "pattern": "^[a-z][a-z0-9-]*$",
            "minLength": 1
          },
          "desktop": {
            "title": "Desktop Section",
            "description": "Desktop configuration section.",
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "background_image_file": {
                "type": "string",
                "pattern": "^assets/.+",
                "description": "Asset path for the desktop background image. Values starting with assets/ are treated as file paths under config/assets."
              },
              "shortcuts_in_menu": {
                "type": "array",
                "description": "List of application shortcut IDs shown in the desktop menu.",
                "items": {
                  "type": "string",
                  "minLength": 1
                },
                "minItems": 1
              }
            }
          },
          "printer": {
            "title": "Printer Section",
            "description": "Printer configuration section.",
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "princh": {
                "type": "object",
                "description": "Collection of princh-integrated printers keyed by a logical printer ID.",
                "minProperties": 1,
                "propertyNames": {
                  "pattern": "^[a-z][a-z0-9_-]*$"
                },
                "additionalProperties": {
                  "$ref": "#/$defs/princh_printer"
                }
              },
              "default_printer": {
                "type": "string",
                "description": "Logical ID of the default printer profile that should be selected by the client.",
                "pattern": "^[a-z][a-z0-9_-]*$"
              },
              "no_ppd": {
                "type": "object",
                "description": "Collection of printer profiles that do not require a PPD file. Keys are logical printer IDs.",
                "minProperties": 1,
                "propertyNames": {
                  "pattern": "^[a-z][a-z0-9_-]*$"
                },
                "additionalProperties": {
                  "$ref": "#/$defs/printer_profile"
                }
              },
              "with_ppd": {
                "type": "object",
                "description": "Collection of printer profiles that require a PPD file. Keys are logical printer IDs.",
                "minProperties": 1,
                "propertyNames": {
                  "pattern": "^[a-z][a-z0-9_-]*$"
                },
                "additionalProperties": {
                  "$ref": "#/$defs/printer_profile_with_ppd"
                }
              }
            }
          },
          "wifi": {
            "title": "Wi-Fi Section",
            "description": "Wi-Fi configuration section.",
            "type": "object",
            "additionalProperties": false,
            "required": ["ssid"],
            "properties": {
              "ssid": {
                "type": "string",
                "description": "SSID of the wireless network the client should connect to.",
                "minLength": 1
              },
              "psk": {
                "type": "string",
                "description": "Pre-shared key for the wireless network. This is sensitive data and should eventually be replaced by a runtime secret reference.",
                "minLength": 1
              },
              "hidden": {
                "type": "boolean",
                "description": "Whether the SSID is hidden and requires active probing by the client."
              }
            }
          }
              }
            }
          }
        }
      }
    }
  },
  "$defs": {
    "princh_printer": {
      "type": "object",
      "description": "Princh printer registration entry.",
      "additionalProperties": false,
      "required": ["display_name", "id"],
      "properties": {
        "display_name": {
          "type": "string",
          "description": "Human-readable printer name shown in Princh-related UX.",
          "minLength": 1
        },
        "id": {
          "description": "Printer identifier provided by Princh.",
          "oneOf": [
            { "type": "integer" },
            { "type": "string", "minLength": 1 }
          ]
        }
      }
    },
    "printer_profile": {
      "type": "object",
      "description": "Common printer profile object.",
      "additionalProperties": false,
      "required": ["display_name", "ip"],
      "properties": {
        "display_name": {
          "type": "string",
          "description": "Human-readable printer name shown to users or used by the target system.",
          "minLength": 1
        },
        "description": {
          "type": "string",
          "description": "Optional longer description of where the printer is located or how it is used.",
          "minLength": 1
        },
        "ip": {
          "type": "string",
          "description": "IPv4 address of the printer that the client should use.",
          "format": "ipv4"
        },
        "protocol": {
          "type": "string",
          "description": "Optional transport protocol used to reach the printer, for example ipp or socket.",
          "minLength": 1
        },
        "settings": {
          "$ref": "#/$defs/printer_settings"
        }
      }
    },
    "printer_profile_with_ppd": {
      "type": "object",
      "description": "Printer profile that requires a PPD file.",
      "additionalProperties": false,
      "required": ["display_name", "ip", "ppd_file"],
      "properties": {
        "display_name": {
          "type": "string",
          "description": "Human-readable printer name shown to users or used by the target system.",
          "minLength": 1
        },
        "description": {
          "type": "string",
          "description": "Optional longer description of where the printer is located or how it is used.",
          "minLength": 1
        },
        "ip": {
          "type": "string",
          "description": "IPv4 address of the printer that the client should use.",
          "format": "ipv4"
        },
        "protocol": {
          "type": "string",
          "description": "Optional transport protocol used to reach the printer, for example ipp or socket.",
          "minLength": 1
        },
        "settings": {
          "$ref": "#/$defs/printer_settings"
        },
        "ppd_file": {
          "type": "string",
          "pattern": "^assets/.+",
          "description": "Asset path to a PPD file. Values starting with assets/ are treated as file paths under config/assets."
        }
      }
    },
    "printer_settings": {
      "type": "object",
      "description": "Optional default print settings for a printer profile.",
      "additionalProperties": false,
      "properties": {
        "page_size": {
          "type": "string",
          "description": "Default page size to use when printing, for example A4."
        },
        "color_mode": {
          "type": "string",
          "description": "Default color mode, for example rgb or grayscale."
        },
        "duplex": {
          "type": "string",
          "description": "Default duplex mode, for example none or duplex_tumble."
        },
        "orientation": {
          "type": "string",
          "description": "Default print orientation, for example portrait or landscape."
        }
      }
    }
  }
}
